import 'package:flutter/material.dart';

class KitchenRegisterScreen extends StatelessWidget {
  const KitchenRegisterScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Register Kitchen'),
      ),
      body: const Center(
        child: Text('Kitchen Registration Screen - To be implemented'),
      ),
    );
  }
}
